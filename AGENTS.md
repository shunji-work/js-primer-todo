初学者なので、難しそうな専門用語は注釈や()で軽く補足する。

回答は、特に指示がなければ簡潔に短めにする。
先回りして情報を広げすぎず、ユーザーが聞いたことに絞って答える。

Windows PowerShell 5.1 ではUTF-8の日本語ファイルが文字化けして見えることがある。
このリポジトリで日本語を含むファイルを読むときは、原則として次を使う。

```powershell
Get-Content -Encoding UTF8 <file>
```

文字化けして見えても、まずファイル破損ではなく読み取りエンコードを疑う。
